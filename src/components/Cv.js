import React, {useState} from "react"

import "./Cv.scss"

export const Cv = ({data}) => {
    return(
        <div className="dc-cv">
            <dl>
                
            </dl>
        </div>
    )
}